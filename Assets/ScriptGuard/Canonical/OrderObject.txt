using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrderObject : MonoBehaviour
{
    public Transform currentSlot; // Reference to the current slot (if any)
    public int currentIndex; // The correct index for this object in the order  
    public int correctIndex; // The correct index for this object in the order  
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
